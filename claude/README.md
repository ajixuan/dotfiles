# Commands
```
./run-claude.sh --azure <path to directory>
./claude-cp.sh <container path relative to project> <host path>
./claude-cp.sh -r <container path relative to project> <host path>
./claude-cp.sh -c <container name> <container path relative to project> <host path>
```

